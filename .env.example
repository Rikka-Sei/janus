PORT="3000"

# Database type: mysql or postgresql
DB_TYPE="mysql"

DB_HOST=
DB_PORT=
DB_USERNAME=
DB_PASSWORD=
DB_NAME=

ISSUER=
BS_SITE_URL=
SHARED_CLIENT_ID=

TOKEN_EXPIRES_IN_1="259200"
TOKEN_EXPIRES_IN_2="604800"
DEVICE_CODE_EXPIRES_IN="600"
GRANT_EXPIRES_IN="25920000"

# STOP HERE! You have completed all necessary editing.
# DO NOT edit the following lines UNLESS you know what you're doing.

# MySQL: mysql://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}
# PostgreSQL: postgresql://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}
DB_CONNECTION_STRING=""
